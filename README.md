# Generated Outputs

Run `medical_diagnosis_ai.py` to generate the project outputs in this folder.

The outputs include the synthetic dataset, model comparison, classification report, confusion matrix, feature importance (when supported), and an example prediction.
